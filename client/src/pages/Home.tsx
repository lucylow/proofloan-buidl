import { useMemo, useState } from "react";
import { ArrowRight, Check, CircleDot, Copy, ExternalLink, FileCheck2, LockKeyhole, ShieldCheck, Sparkles, WalletCards, X } from "lucide-react";
import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { PROOFLOAN_STATES, type SourceChain } from "@shared/proofloan";

const demoWallet = "0x71C7...9A2F";

export default function Home() {
  const [walletAddress, setWalletAddress] = useState(demoWallet);
  const [sourceChain, setSourceChain] = useState<SourceChain>("Ethereum Sepolia");
  const [applicationId, setApplicationId] = useState<string | null>(null);
  const [copied, setCopied] = useState(false);
  const createApplication = trpc.proofloan.createApplication.useMutation({ onSuccess: data => setApplicationId(data.applicationId) });
  const acceptOffer = trpc.proofloan.acceptOffer.useMutation();
  const applicationQuery = trpc.proofloan.getApplication.useQuery({ applicationId: applicationId ?? "_none_" }, { enabled: Boolean(applicationId), refetchInterval: applicationId ? 5000 : false });
  const app = applicationQuery.data;
  const currentIndex = useMemo(() => PROOFLOAN_STATES.indexOf(app?.state ?? "Intake"), [app?.state]);

  const startProof = () => createApplication.mutate({ walletAddress, sourceChain });
  const copyId = async () => { if (!app) return; await navigator.clipboard?.writeText(app.applicationId); setCopied(true); setTimeout(() => setCopied(false), 1200); };

  return (
    <div className="min-h-screen bg-[#070b12] text-slate-100 selection:bg-cyan-300 selection:text-slate-950">
      <header className="sticky top-0 z-20 border-b border-white/10 bg-[#070b12]/90 backdrop-blur-xl">
        <div className="mx-auto flex max-w-7xl items-center justify-between px-6 py-4">
          <a href="#top" className="flex items-center gap-3"><div className="grid h-9 w-9 place-items-center rounded-xl bg-cyan-300 text-slate-950 shadow-[0_0_28px_rgba(103,232,249,.25)]"><ShieldCheck size={20} /></div><div><div className="font-black tracking-tight">ProofLoan</div><div className="text-[10px] uppercase tracking-[0.24em] text-slate-500">Creditcoin underwriting</div></div></a>
          <nav className="hidden items-center gap-7 text-sm text-slate-400 md:flex"><a href="#how-it-works" className="transition hover:text-white">How it works</a><a href="#evidence" className="transition hover:text-white">Evidence</a><a href="#docs" className="transition hover:text-white">Docs</a></nav>
          <Button onClick={() => document.getElementById("apply")?.scrollIntoView({ behavior: "smooth" })} className="rounded-full bg-cyan-300 px-5 font-bold text-slate-950 hover:bg-cyan-200">Start a loan <ArrowRight size={16} /></Button>
        </div>
      </header>

      <main id="top" className="mx-auto max-w-7xl px-6">
        <section className="grid gap-12 py-20 lg:grid-cols-[1.05fr_.95fr] lg:items-center lg:py-28">
          <div><Badge className="mb-6 border border-cyan-300/30 bg-cyan-300/10 text-cyan-200">ATTESTCOIN PROTOCOL × CREDITCOIN</Badge><h1 className="max-w-3xl text-5xl font-black leading-[.96] tracking-[-.055em] text-white sm:text-7xl">Credit that starts with <span className="text-cyan-300">proof.</span></h1><p className="mt-7 max-w-2xl text-lg leading-8 text-slate-400">ProofLoan turns verified cross-chain behavior into a bounded credit decision. Every fact, model output, policy check, and Creditcoin execution is visible in one auditable trail.</p><div className="mt-9 flex flex-wrap gap-4"><Button onClick={() => document.getElementById("apply")?.scrollIntoView({ behavior: "smooth" })} className="h-12 rounded-full bg-cyan-300 px-6 font-bold text-slate-950 hover:bg-cyan-200">Prove my history <ArrowRight size={18} /></Button><a href="#how-it-works" className="inline-flex h-12 items-center gap-2 rounded-full border border-white/15 px-6 font-semibold text-slate-200 transition hover:border-cyan-300/50 hover:bg-white/5">See the trust boundary</a></div><div className="mt-10 flex flex-wrap gap-5 text-xs text-slate-500"><span className="flex items-center gap-2"><LockKeyhole size={14} className="text-cyan-300" /> No opaque oracle</span><span className="flex items-center gap-2"><FileCheck2 size={14} className="text-cyan-300" /> Typed evidence</span><span className="flex items-center gap-2"><Sparkles size={14} className="text-cyan-300" /> AI advisory, not signer</span></div></div>
          <div className="relative overflow-hidden rounded-[2rem] border border-white/10 bg-[#0b121d] p-5 shadow-2xl shadow-cyan-950/20"><div className="absolute -right-20 -top-20 h-64 w-64 rounded-full bg-cyan-300/10 blur-3xl" /><div className="relative rounded-3xl border border-white/10 bg-[#0d1622] p-6"><div className="flex items-center justify-between"><span className="text-xs uppercase tracking-[.22em] text-slate-500">Live underwriting trace</span><span className="flex items-center gap-2 text-xs text-emerald-300"><span className="h-2 w-2 rounded-full bg-emerald-300" /> Testnet ready</span></div><div className="mt-8 space-y-4"><TraceRow label="Source transaction" value="Ethereum Sepolia" done /><TraceRow label="Attestcoin proof" value="VerifiedFact · 3 records" done /><TraceRow label="AI decision" value="Risk tier B · 92% confidence" done /><TraceRow label="RiskGuard" value="Offer within bounds" done /></div><div className="mt-8 rounded-2xl border border-cyan-300/20 bg-cyan-300/5 p-4"><div className="text-xs text-slate-500">Indicative offer</div><div className="mt-1 flex items-baseline justify-between"><span className="text-4xl font-black text-white">1,500 <small className="text-base text-slate-400">USDC</small></span><span className="text-right text-sm font-semibold text-cyan-200">11.5% APR<br /><span className="text-xs font-normal text-slate-500">90 day term</span></span></div></div></div></div>
        </section>

        <section id="how-it-works" className="border-t border-white/10 py-16"><div className="mb-9 max-w-2xl"><p className="text-xs font-bold uppercase tracking-[.24em] text-cyan-300">One bounded loop</p><h2 className="mt-3 text-3xl font-black tracking-tight text-white sm:text-4xl">Evidence → inference → policy → execution.</h2></div><div className="grid gap-4 md:grid-cols-4">{[["01", "Prove", "The Attestcoin proof worker returns verified, typed facts from a source chain."], ["02", "Score", "A server-side model builds a FeatureVector and produces calibrated PD plus reasons."], ["03", "Guard", "RiskGuard deterministically checks amount, LTV, rate, freshness, confidence, and liquidity."], ["04", "Execute", "Only an accepted, bounded offer crosses the Creditcoin testnet execution boundary."]].map(([n, title, body]) => <div key={n} className="rounded-3xl border border-white/10 bg-white/[.03] p-5"><div className="text-xs font-bold text-cyan-300">{n}</div><h3 className="mt-8 text-xl font-bold text-white">{title}</h3><p className="mt-3 text-sm leading-6 text-slate-400">{body}</p></div>)}</div></section>

        <section id="apply" className="grid gap-6 py-12 lg:grid-cols-[.88fr_1.12fr]"><Card className="border-white/10 bg-[#0b121d] text-white"><CardHeader><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.22em] text-cyan-300">Borrower intake</p><CardTitle className="mt-2 text-2xl">Start your proof request</CardTitle></div><WalletCards className="text-cyan-300" /></div></CardHeader><CardContent className="space-y-5"><label className="block text-sm font-medium text-slate-300">Wallet address or source tx hash<input placeholder="0x… wallet address or 32-byte tx hash" value={walletAddress} onChange={e => setWalletAddress(e.target.value)} className="mt-2 h-12 w-full rounded-xl border border-white/10 bg-black/20 px-4 font-mono text-sm text-white outline-none ring-cyan-300/40 focus:ring-2" /></label><label className="block text-sm font-medium text-slate-300">Source chain<select value={sourceChain} onChange={e => setSourceChain(e.target.value as SourceChain)} className="mt-2 h-12 w-full rounded-xl border border-white/10 bg-[#111a28] px-4 text-sm text-white outline-none ring-cyan-300/40 focus:ring-2"><option>Ethereum Sepolia</option><option>Polygon Amoy</option></select></label><Button disabled={createApplication.isPending || walletAddress.length < 8} onClick={startProof} className="h-12 w-full rounded-xl bg-cyan-300 font-bold text-slate-950 hover:bg-cyan-200">{createApplication.isPending ? "Verifying with Attestcoin…" : "Fetch and verify history"} <ArrowRight size={17} /></Button><p className="text-xs leading-5 text-slate-500">Paste a mined 32-byte source transaction hash to run the official @gluwa/usc-sdk ProofBuilder + Creditcoin BlockProver path. A wallet address uses the labeled preview adapter. Raw RPC data is never treated as financial truth.</p>{createApplication.error && <p role="alert" className="rounded-xl border border-rose-300/20 bg-rose-400/10 p-3 text-xs text-rose-200">Proof request failed: {createApplication.error.message}</p>}</CardContent></Card>

          <Card className="border-white/10 bg-[#0b121d] text-white" id="evidence"><CardHeader><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-[.22em] text-cyan-300">Borrower dashboard</p><CardTitle className="mt-2 text-2xl">Your verifiable credit file</CardTitle></div>{app && <Badge className={app.state === "Rejected" ? "bg-rose-400/10 text-rose-300" : "bg-emerald-400/10 text-emerald-300"}>{app.state}</Badge>}</div></CardHeader><CardContent>{!app ? <div className="grid min-h-[270px] place-items-center rounded-2xl border border-dashed border-white/10 text-center"><div><CircleDot className="mx-auto mb-3 text-slate-600" /><p className="font-semibold text-slate-300">No application in progress</p><p className="mt-2 text-sm text-slate-500">Submit a wallet proof request to populate your evidence graph.</p></div></div> : <div className="space-y-6"><div className="flex items-center gap-2 overflow-x-auto pb-1">{PROOFLOAN_STATES.map((state, i) => <div key={state} className={`flex shrink-0 items-center gap-2 text-xs ${i <= currentIndex ? "text-cyan-200" : "text-slate-600"}`}><span className={`grid h-6 w-6 place-items-center rounded-full border ${i < currentIndex ? "border-cyan-300 bg-cyan-300 text-slate-950" : i === currentIndex ? "border-cyan-300 text-cyan-200" : "border-white/10"}`}>{i < currentIndex ? <Check size={13} /> : i + 1}</span><span className="hidden sm:inline">{state}</span>{i < PROOFLOAN_STATES.length - 1 && <span className="mx-1 h-px w-5 bg-white/10" />}</div>)}</div><Tabs defaultValue="evidence"><TabsList className="bg-white/5"><TabsTrigger value="evidence">VerifiedFact</TabsTrigger><TabsTrigger value="decision">Decision</TabsTrigger><TabsTrigger value="audit">Audit trail</TabsTrigger></TabsList><TabsContent value="evidence" className="mt-5 space-y-3">{app.facts.map(fact => <div key={fact.id} className="rounded-2xl border border-white/10 bg-black/15 p-4"><div className="flex flex-wrap items-center justify-between gap-2"><div className="flex items-center gap-2"><Badge className="bg-emerald-400/10 text-emerald-300">{fact.eventType}</Badge><span className="font-semibold text-white">{fact.amount}</span></div><span className="text-xs text-emerald-300">{fact.freshness}</span></div><div className="mt-3 grid gap-3 text-xs text-slate-400 sm:grid-cols-2"><div>Chain <b className="ml-2 font-medium text-slate-200">{fact.chain}</b></div><div>Source block <b className="ml-2 font-medium text-slate-200">{fact.sourceBlock.toLocaleString()}</b></div><div>Tx hash <b className="ml-2 font-mono font-medium text-slate-200">{fact.txHash}</b></div><div>USC verification block <b className="ml-2 font-medium text-slate-200">{fact.verificationBlock.toLocaleString()}</b></div></div><div className="mt-3 flex items-center gap-2 border-t border-white/10 pt-3 text-[11px] text-slate-500"><FileCheck2 size={13} className="text-cyan-300" /> {fact.proofWorker} · root {fact.proofRoot}</div></div>)}</TabsContent><TabsContent value="decision" className="mt-5">{app.decision && app.offer ? <div className="space-y-4"><div className="grid gap-3 sm:grid-cols-3"><Metric label="30d PD" value={`${(app.decision.pd30 * 100).toFixed(1)}%`} /><Metric label="Confidence" value={`${(app.decision.confidence * 100).toFixed(0)}%`} /><Metric label="Risk tier" value={app.decision.riskTier} /></div><div className="rounded-2xl border border-cyan-300/20 bg-cyan-300/5 p-4"><div className="flex items-center justify-between"><div><div className="text-xs uppercase tracking-[.18em] text-slate-500">RiskGuard offer</div><div className="mt-1 text-3xl font-black text-white">{app.offer.amount.toLocaleString()} USDC</div></div><div className="text-right text-sm text-cyan-200">{app.offer.apr}% APR<br /><span className="text-xs text-slate-500">{app.offer.termDays} days · {(app.offer.ltv * 100).toFixed(0)}% LTV</span></div></div>{app.offer.status === "Ready" && <Button disabled={acceptOffer.isPending} onClick={() => acceptOffer.mutate({ applicationId: app.applicationId })} className="mt-5 h-11 w-full rounded-xl bg-cyan-300 font-bold text-slate-950 hover:bg-cyan-200">Accept and submit on Creditcoin <ArrowRight size={16} /></Button>}{app.state === "Executed" && <div className="mt-4 flex items-center gap-2 rounded-xl bg-emerald-400/10 p-3 text-sm text-emerald-300"><Check size={16} /> Creditcoin testnet execution recorded.</div>}{acceptOffer.error && <p role="alert" className="mt-4 rounded-xl border border-rose-300/20 bg-rose-400/10 p-3 text-xs text-rose-200">Acceptance failed: {acceptOffer.error.message}</p>}</div><div className="flex flex-wrap gap-2">{app.decision.reasonCodes.map(reason => <Badge key={reason} className="border border-white/10 bg-white/5 text-slate-300">{reason}</Badge>)}</div></div> : <p className="text-sm text-slate-500">Decision is being assembled from verified evidence.</p>}</TabsContent><TabsContent value="audit" className="mt-5 space-y-3">{app.audit.map(event => <div key={event.hash} className="flex gap-3 rounded-xl border border-white/10 bg-black/10 p-3"><div className="mt-1 h-2 w-2 shrink-0 rounded-full bg-cyan-300" /><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><span className="font-semibold text-white">{event.label}</span><span className="text-[11px] text-slate-600">{new Date(event.timestamp).toLocaleTimeString()}</span></div><p className="mt-1 text-sm text-slate-400">{event.detail}</p><code className="mt-2 block truncate text-[10px] text-slate-600">event hash {event.hash}</code></div></div>)}{app && <button onClick={copyId} className="flex items-center gap-2 text-xs text-cyan-300 hover:text-cyan-200">{copied ? <Check size={13} /> : <Copy size={13} />} {copied ? "Application ID copied" : `${app.applicationId} · copy ID`}</button>}</TabsContent></Tabs></div>}</CardContent></Card></section>

        <section id="docs" className="border-t border-white/10 py-16"><div className="grid gap-8 lg:grid-cols-[.8fr_1.2fr]"><div><p className="text-xs font-bold uppercase tracking-[.24em] text-cyan-300">Technical docs</p><h2 className="mt-3 text-3xl font-black tracking-tight text-white">The trust boundary is the product.</h2><p className="mt-4 max-w-xl leading-7 text-slate-400">ProofLoan keeps evidence, inference, policy, and execution separate. The AI can be wrong without gaining authority to move funds; the Attestcoin proof worker can be unavailable without silently turning raw RPC into truth.</p><a href="https://docs.creditcoin.org/attestcoin-protocol/dapp-builder-infrastructure/attestcoin-sdk-usc-sdk.md" target="_blank" rel="noreferrer" className="mt-6 inline-flex items-center gap-2 text-sm font-semibold text-cyan-300">Read Attestcoin Protocol USC SDK docs <ExternalLink size={15} /></a></div><div className="rounded-3xl border border-white/10 bg-[#0b121d] p-5"><pre className="overflow-x-auto text-sm leading-7 text-slate-400"><span className="text-cyan-300">SourceTransaction</span>  →  <span className="text-emerald-300">VerifiedFact</span>  →  <span className="text-purple-300">FeatureVector</span>  →  <span className="text-amber-300">Decision</span>  →  <span className="text-pink-300">Offer</span>  →  <span className="text-cyan-300">Creditcoin Loan</span>{"\n\n"}<span className="text-slate-500">USC verifier</span>       evidence authority{"\n"}<span className="text-slate-500">AI model</span>          advisory inference{"\n"}<span className="text-slate-500">RiskGuard</span>         deterministic policy{"\n"}<span className="text-slate-500">LoanPool</span>         bounded execution</pre></div></div></section>
      </main>
      <footer className="border-t border-white/10"><div className="mx-auto flex max-w-7xl flex-wrap items-center justify-between gap-3 px-6 py-6 text-xs text-slate-500"><span>ProofLoan · built for BUIDL CTC 2026 Fall</span><span>Attestcoin Protocol integration · Creditcoin testnet demo</span></div></footer>
    </div>
  );
}

function TraceRow({ label, value, done }: { label: string; value: string; done?: boolean }) { return <div className="flex items-center gap-3"><div className={`grid h-8 w-8 place-items-center rounded-full ${done ? "bg-emerald-400/15 text-emerald-300" : "bg-white/5 text-slate-500"}`}>{done ? <Check size={15} /> : <CircleDot size={15} />}</div><div className="flex-1"><div className="text-sm text-slate-300">{label}</div><div className="text-xs text-slate-500">{value}</div></div></div>; }
function Metric({ label, value }: { label: string; value: string }) { return <div className="rounded-2xl border border-white/10 bg-black/15 p-4"><div className="text-xs text-slate-500">{label}</div><div className="mt-1 text-2xl font-black text-white">{value}</div></div>; }
